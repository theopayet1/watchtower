# Collecte
requests>=2.31
feedparser>=6.0

# Config
PyYAML>=6.0
python-dotenv>=1.0

# Synthèse (étape 6)
anthropic>=0.40

# État / dédoublonnage (étape 5) — backend Supabase (mode cloud)
supabase>=2.0
